<div class="invx-sidebar">
  <ul class="menu">
    {% for link in linklists['page-sidebar'].links %}
      <li class="menu-link">
        <a href="{{ link.url }}">{{ link.title }}</a>
      </li>
    {% endfor %}
  </ul>
</div>
